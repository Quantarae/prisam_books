Sleepy Squirrel Secret (placeholder)

This folder contains placeholder assets for the story "The Case of the Sleepy Squirrel’s Secret".

Structure:
- `images/` — put `page_0.png` .. `page_9.png` here.
- `audio/` — put narration files (e.g. `page_0.mp3`) here when ready.
- `lottie/` — optional per-page Lottie animation JSON files.
- `story.json` — the per-page metadata (already present). Fill `title` and `text` fields.

Notes:
- Filenames in `story.json` are relative to this folder. Example: `images/page_0.png`, `audio/page_0.mp3`.
- Do not change the `story.json` structure; only fill `title`, `text`, and provide audio/image files.
