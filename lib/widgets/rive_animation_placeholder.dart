import 'package:flutter/material.dart';

class RiveAnimationPlaceholder extends StatelessWidget {
  const RiveAnimationPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.animation, size: 120, color: Colors.black26),
    );
  }
}
